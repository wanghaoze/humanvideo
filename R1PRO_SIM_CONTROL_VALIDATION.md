# Quest 3 → R1 Pro 仿真控制验收（2026-09-15）

通过范围：真实 Quest 3 双手柄输入，经本机 ADB/HTTP 发送到服务器 MuJoCo，驱动左右机械臂与夹爪。没有连接真实机器人，没有开始训练数据录制。

## 接口及坐标检查

`runs/sim_control_probe_20260915_v2/report.json` 的 12 项检查均通过：分别驱动左右臂、另一臂目标保持、夹爪闭合、松侧握键保持目标、输入超时、暂停保持及关节跟随。

按本地同版本场景模型进行正运动学计算，左右手柄分别向外移动 4 cm 的合成测试使 TCP 沿机器人 Y 轴分别移动 +2.7605 cm 和 -2.7544 cm，与映射倍率 0.7 的预期约 2.8 cm 一致。这不是全工作空间或所有姿态的标定。

## 实时硬件检查

第一次实时测试没有触发侧握键，因此未通过运动验收；记录保留于 `runs/quest_sim_live_20260915_01`。

第二次 30 秒测试通过，结果位于 `runs/quest_sim_live_20260915_02`：

- 335 个真实 Quest 控制包被服务器成功接受，无 HTTP 错误。
- 左右侧握键与食指扳机均覆盖 0–1。
- 左臂单关节最大变化范围约 0.200 rad，右臂约 0.401 rad。
- 左右夹爪开度均覆盖约 0.004–0.100 m。
- 停止发送后服务器检测输入过期，控制目标保持不变。
- Quest 应用已停止；最终服务器状态 ready=true、paused=true、recording=false、input_fresh=false。

HTTP 请求耗时中位数约 32 ms，仅为本机到服务接口的请求耗时，不是传感器到机器人运动的端到端延迟。本轮有效发送率约 11 包/秒，未达到仿真 20 Hz 的逐帧输入；脚本同时抓图和查询状态，因此不能用作纯传输吞吐基准。

`simulation_preview.mp4` 来自服务器实际返回的图像，按抓图时刻重采样为 5 fps；原始抓图频率约 2 Hz。它是画面预览，不是 MuJoCo 状态重新仿真得到的物理回放。

## 使用与后续

中指按住侧握键启用对应机械臂；保持握住时，食指扳机控制夹爪。松开侧握键后保持目标，重新握住会重新建立相对移动起点。

```powershell
cd C:\Users\HAOZEW\Documents\code\humanvideo
.\.venv-r1pro\Scripts\python.exe scripts/verify_quest_sim.py --seconds 30 --output runs/quest_sim_next_test
```

头显应已唤醒并佩戴，输出目录必须是新目录。该验证命令会重置空闲仿真、屏蔽录制快捷键，并在结束后暂停仿真。

本次没有验证托盘抓取、搬运、放下。默认场景的可达性与在线 IK 问题仍然存在，详见 `R1PRO_SERVER_OPTIMIZATION.md`。下一步应修正工位和接近姿态，再验证无碰撞抓取路径与稳定夹持。当前数据不是搬盘成功示教，也不是仿真到真机迁移验证。
