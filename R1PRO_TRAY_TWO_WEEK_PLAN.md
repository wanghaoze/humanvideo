# R1 Pro 单个空托盘搬运：两周推进与验收计划

制定日期：2026-09-10。状态：待执行的工程计划；本文中的控制适配器、训练结果和真机验收结果尚未实现或验证。

**目标：在第二周完成 ACT 策略控制 R1 Pro 的受控真机部署，并交付可复现的数据、模型、启动流程和测试报告。** 第一周完成控制接口、数据链路和首个模型，第二周开始上机，留下纠错时间。

以下数量、成功率、误差和时间门槛是本项目建议的验收目标，不是文献结论，也不是对训练结果的保证。D1–D10 表示十个主要工作日；两周内另四天用于离线训练、资料整理或机时缓冲，不默认安排周末上机。

## 1. 范围、条件与路线

**本期任务**：固定工位、固定底盘与躯干、一种实测尺寸的空托盘，从 A 区双臂拿起、平稳搬运、放到固定 B 区、松手并撤离。默认夹取托盘两侧，D1 用实物验证夹爪是否适合。源位置允许有限变化，目的区域保持固定。

D1 提议源位置相对名义点在 X/Y 各 ±50 mm、偏航角 ±10° 内变化；D2 根据可达性与夹持验证冻结最终范围。此后扩大或缩小范围都必须记录为规格变更，不把缩小后的成绩冒充原范围成绩。

本期交付面向隔离测试工位、有操作员在场的部署。叠盘分离、装有手机/背包的托盘、移动底盘、追踪运动传送带、乘客近距离交互另立任务，不能由本期结果推断能力。

**资源假设**：一名全职工程人员，真机试验时有熟悉遥操作与急停的人员配合；D1–D3、D6–D10 每天约 2–4 小时机器人使用时间；已有可用遥操作、合适的托盘和相机；可预约一张服务器 GPU。若上述条件缺失，应相应顺延，而非压缩上机验证。用户此前 GPU 截图不能代表当前空闲资源，启动任务前重新确认。

| 技术路径 | 两周内的用途 | 时间边界与验收 |
|---|---|---|
| 示教航点 + 状态反馈 | 固定名义位置的控制基线，验证抓取、执行和放置是否可行 | D2 名义工况 10 次至少 9 次成功；只在已验证的工况使用 |
| ACT + 机器人遥操作数据 | 唯一主训练路线；学习图像和本体状态到动作的映射 | D5 首个模型，D6 真机首测，D9 正式验收 |
| 人类视频的阶段与接触分析 | 确定操作步骤、可夹区域、失败分类与示教覆盖范围 | 总计约半天；D4 有 5 段相关视频的人工核验标注 |
| 现有 TRELLIS.2 资产 | 辅助可视化和物体语义记录 | 最多半天；抓取尺寸、尺度、碰撞边界以实测为准 |
| π 系列或 Diffusion Policy | 后续对照候选 | 两周内只做路线评估；只有主线提前达标且适配器现成时，允许额外半天离线兼容性试验，不取代正式验收 |
| 强化学习、高保真仿真、完整人手动作重定向 | 下一阶段研究 | 本期不列入部署依赖 |

选择 ACT 的理由是它已有多相机/本体状态输入和动作分块的实现，适合作为已有遥操作数据的第一条可复现基线；这不代表 ACT 在 R1 Pro 托盘任务上已被证明优于其他模型。参见 [LeRobot ACT 官方文档](https://huggingface.co/docs/lerobot/act)。π 系列使用本机数据仍涉及数据转换、动作空间、归一化与部署适配，预训练不等于直接兼容 R1 Pro，参见 [openpi 官方仓库](https://github.com/Physical-Intelligence/openpi)。

## 2. D1–D2 冻结的接口与验收协议

**环境与运行位置**：保留已完成的 `humanvideo-trellis2` 重建环境。ACT 训练/推理使用独立环境，记录依赖和代码提交。第一版可以在服务器推理、机器人侧本地执行；必须测量整个网络闭环。完整机载推理不作为本期必达项。

你给出的 `ROS_VERSION=2 / ROS_DISTRO=jazzy` 是运行该命令的服务器环境信息。官方公开 R1 Pro 指南描述了 Ubuntu 22.04 / ROS 2 Humble，因此 D1 必须以真机当前 SDK 和支持接口为准。优先复用已能遥操作的原厂运行环境，通过明确的适配接口连接策略，不能直接假定两个 ROS 发行版的部署已经打通，也不为此改动现有真机系统。参考 [R1 Pro 官方 ROS 2 指南](https://docs.galaxea-ai.com/Guide/R1Pro/software_introduction/R1Pro_Software_Guide_ROS2/)。

接口清单必须记录：

- 真机、遥操作主机、训练服务器分别使用的 OS、ROS、SDK 版本与网络连接。
- 双臂、夹爪的实际关节名称/顺序/单位/反馈频率；动作是关节目标还是末端目标，绝对值还是增量。
- 相机数量、分辨率、颜色通道顺序、安装位置、时间戳来源和更新频率。只使用已工作的相机；不依赖新采购。位姿驱动的模块另需验证相机到机器人坐标系的标定。
- 观测采集、策略推理、指令发送三个频率；动作分块长度、每次实际执行的步数、最长开环时间和指令有效期。
- 控制权仲裁、启动/停止接口、跟踪误差门槛、关节与速度限制，以及双臂异常时共同停止的语义。

**数据契约**：原始数据保留 ROS bag 或现有遥操作套件的等价原始记录，再转换成训练格式。

| 字段 | 内容与核验要求 |
|---|---|
| 图像 | 现有固定相机和可用腕部相机；保留原始采集时间 |
| `observation.state` | 实际测量的双臂关节状态和夹爪状态；维度由 SDK 核实，不预先写死 |
| `action` | 对应时间实际发送并由执行链路接受的目标；同时保留限幅前后值便于排查 |
| 时间 | 采集、接收、指令发送时间及钟源；保存同步误差和丢帧报告 |
| 元信息 | episode/session ID、操作者、初始位姿、模型/控制器版本、结果与失败阶段 |
| 可选辅助信息 | SDK 确实提供时记录电流/力/末端位姿；不假定存在力矩或触觉传感器 |

若执行接口是末端位姿，不能为了套用示例就把实测关节角直接当作关节动作标签。先明确执行器收到的目标与训练输出如何一一对应。归一化统计只由训练集计算。

**执行保护**：本地执行器独占控制权，检查指令新鲜度、输入有效性、目标范围和连续性。图像失效、推理进程退出、网络中断、异常目标输入应进入已验证的受控停止/保持状态；暂停不能默认变成夹爪张开，也不能等同于断力矩。先空载或在托盘有支撑的状态验证故障路径。软件限制不替代原厂控制保护和现场急停。

**正式成功定义**：机器人自主完成拿起、搬运、放置、松手、撤离；托盘完整支撑在 B 区，松手后稳定至少 2 秒；无人接管、无掉盘、无非预期碰撞。人工复位可以发生在两次任务之间，任务过程中接管计该次失败。

建议放置误差为托盘中心 ≤30 mm、偏航误差 ≤10°，用工位标记、量尺或经核验的测量系统记录；D2 根据实际 B 区尺寸冻结。记录每次耗时，建议成功任务的中位耗时 ≤安全遥操作基线中位耗时的 1.5 倍。单次超时门槛也在 D2 预先确定。

## 3. 每个工作日的推进与验收

### D1：确认实物能搬、接口能用

**工作**：完成接口清单；量托盘长宽高、边缘厚度、质量，确定夹持面与放置面；固定工作台、底盘、躯干和相机；确认遥操作控制权与急停；使用现有遥操作完成名义位置搬运。

**产物**：`interface_contract.yaml`、`task_spec.yaml`、实测尺寸与工位照片、5 次遥操作记录。文件名是计划交付名，并非当前已有实现。

**验收 G1**：5/5 次完整遥操作成功；左右臂和夹爪状态可观察，控制权切换清楚；源区、目标区均可达；停止操作经现场验证。

**未通过**：先解决夹爪、夹持位置、工作台或 SDK 问题。若需要新机械件或厂商修复，重新评估两周工期；不开始批量采集来掩盖机械不可行性。

### D2：建控制基线与本地执行器

**工作**：基于原厂支持接口实现状态读取、控制权仲裁和目标发送；用已验证航点及状态反馈完成名义位置搬运；接入限幅、超时、人工停止和双臂异常处理。D1 的遥操作录像不等于可直接无反馈重放的安全轨迹。

**产物**：`robot_adapter`、`executor`、名义位置基线配置、故障测试日志；冻结任务范围与所有验收参数。

**验收 G2**：名义位置 10 次至少 9 次成功，零掉盘、零非预期碰撞；空载检查推理服务退出/网络中断、无效或过期指令、停止请求等故障路径，均按冻结的超时门槛停止接受旧目标；重启后不会自动运动。左右臂不会分别接受来自不同控制源的命令。

**未通过**：D3 优先修控制。受控停止问题未解决，不进入策略带物体试验。若要缩小源区，只能更新规格并重新通过基线。

### D3：小样本打通采集、转换、训练和部署格式

**工作**：录制 10 条试采集，转换为所选固定版本的 LeRobot 格式；制作同步回放和数据质量报告；用小数据走通训练一个短作业、保存、加载、推理。短作业仅验证管线，不评判任务能力。

**产物**：原始记录、转换脚本、10 条试采集数据、质量报告、动作/状态时间曲线和可加载的管线检查模型。

**验收 G3**：10/10 条通过字段、维度、单位、左右顺序和有限数值检查；人工抽查全部 10 条回放中夹爪开合与图像一致；时间戳单调，动作标签含义正确。建议对齐后 99% 图像—状态时间差不超过一个相机采样周期；原始缺失、重复、时钟漂移单列，不能靠填充掩盖。最终预算以 D2 记录为准。

**未通过**：只修同步或数据转换，暂停批量录制。低训练 loss 不能作为数据有效的证据。

### D4：首批有效示教与人类视频分析

**工作**：按冻结范围采集约 40 条完整成功示教，覆盖源位置和朝向变化；按完整 episode 与采集 session 分组划分训练/验证。分析 5 段真实托盘人类示范视频，标记“接近、夹持、抬起、搬运、落稳、松手”与可见接触区域；缺少对应视频时现场拍摄，文件盒视频只用来借鉴流程。启动 ACT 第一轮训练。

**产物**：数据集 v0、划分清单、训练配置、5 段阶段/接触标注、首轮训练日志。

**验收 G4**：至少 40 条成功数据通过 G3 的自动检查；至少 10 条人工复核；验证集来自独立 session，不能把同条轨迹相邻帧分到两侧。5 段人类视频均有人核验，遮挡的接触区域标为未知。训练能保存并加载 checkpoint，loss 无 NaN。

**未通过**：优先提高数据质量和覆盖率；人类视频自动分析或 3D 美化累计超过半天仍无成果时改用人工标注，保证 ACT 主线继续。

### D5：完成第一版训练并做离线部署检查

**工作**：累计约 80 条有效成功示教，建议约 64 条训练、16 条验证，并保留 session 隔离；冻结数据集 v1。完成 ACT v0，使用录制流或在线观测运行部署进程，但不发送运动命令，检查反归一化、动作顺序、输出幅度、相机预处理与时延。基线控制也使用相同观测/执行记录规范。

**产物**：checkpoint、训练集归一化统计、配置与版本清单、连续 10 分钟推理日志、第一版路线选择记录。

**验收 G5**：新进程能够加载全部部署文件；无非法数值、单位错误、左右映射错误或无界动作跳变；完整的观测到可执行输出延迟在 D2 预算内，动作缓冲不会耗尽。输出平滑性必须看分块边界。离线动作误差只用于排错，不能宣布真实任务成功。

**未通过**：定位到数据、模型、归一化、网络或适配层之一。先减小输入或调整执行节奏并重验；不因此立即切换大模型。模型步数以验证曲线和保存点选择，不把固定训练时长当作质量保证。

**第一周缓冲**：无人值守只运行离线训练、转码或日志分析。D1–D3 的接口问题若消耗缓冲，应立即取消所有可选模型实验。

### D6：ACT 首次受控真机闭环

**工作**：先做空载/有支撑条件下的动作检查，再在冻结的安全速度与范围内完整搬盘；操作员随时可接管。记录全部尝试，不只保存成功片段。此时是真机实际运行，策略推理可在服务器。

**产物**：10 次开发测试的视频、状态/动作日志、每次结果和失败阶段表。

**验收 G6**：10 次至少 7 次独立完成完整任务，零掉盘、零非预期碰撞；接管计失败。发现异常碰撞或失稳迹象立即停止并排查，不为完成次数继续尝试。

**未通过**：选出现频率最高的一个问题处理：抓偏查视觉/覆盖，双臂不同步查时间和执行，抬起滑落查夹持，放置失败查示教与松手时机。G2 基线若同时失败，优先修硬件/控制而非训练。

### D7：针对失败补数据与重训

**工作**：针对 D6 的具体失败初态补录约 20–30 条完整成功示教；保持观测与动作定义不变；训练 ACT v1。人工接管轨迹须明确专家控制区间，不能把模型失败动作自动标成专家动作；本期优先重新录制干净的完整成功轨迹。

**产物**：数据集 v2、故障频次对比、ACT v1 与新增数据清单。

**验收 G7**：20 次开发测试至少 16 次成功，零掉盘/非预期碰撞；报告各阶段失败数量，确认主要失败类别是否下降。新增数据不进入原验证集，也不来自正式留出测试的失败回放。

**未通过**：停止可选研究。判断两天内可修复的主因；若需要新抓具、未提供的控制接口或明显更大范围训练，报告达标风险，不用换模型名称代替原因分析。

### D8：范围内鲁棒性检查与版本冻结

**工作**：在源位姿边界、组合变化及正常照明变化下完成开发测试；核验重启、停止、掉线恢复和操作记录。冻结模型、统计、相机配置、执行参数及正式试验清单。

**产物**：候选发布包 v1、20 次开发测试、50 次正式试验的预设初态/随机种子与判定表。

**验收 G8**：20 次至少 18 次成功，零掉盘/非预期碰撞；所有运行文件可追溯到版本；断线恢复后需要重新显式启动任务，不自动续跑过期动作。正式初态不得来自训练 episode 的回放，正式试验不参与调参。

**未通过**：修最后一个明确问题并重验；需要缩小范围时，必须重新声明范围、保留原目标未达成的记录。D8 以后不引入新策略家族。

### D9：正式留出真机验收

**工作**：按预登记顺序完成最多 50 次任务。五组各 10 次：名义条件、X 向变化、Y 向变化、偏航变化、范围内组合变化；具体值在 D8 冻结且在训练支持的范围内。可包含经 D2 定义的正常照明差异。由独立操作者或依据完整日志复核。

**产物**：每次结果 CSV、完整视频与执行日志、分组成功率、放置误差、耗时、接管和故障统计。

**验收 G9**：至少 45/50 次成功，且每组至少 8/10；零掉盘、零非预期碰撞；成功定义、误差和耗时符合 D2 冻结协议。发生危险事件应终止测试，该版本记为未通过，不能凑满试验次数。50 次是工程验收样本，不能证明机场长期运行可靠性。

**未通过**：如实输出未通过报告。修复后使用新版本与新的独立测试重新评估，不能删除失败项、拼接不同版本的成功次数或把调参试验计入最终分母。

### D10：复现、交接与下一阶段决策

**工作**：在不重装真机系统的前提下，用干净的策略进程按文档加载依赖与模型；验证版本匹配、显式启动和停止流程；完成最终演示和故障恢复演示。检查所有数据/模型/日志的路径与校验信息。

**产物**：完整部署包、数据清单、操作手册、验收报告、未解决问题与下一阶段实验建议。

**验收 G10**：至少 3 次独立进程重启均能正确加载且不自行发起运动；另一位操作者按文档完成总计 10 次追加任务、至少 9 次成功。该组单独报告，不并入 D9 的 50 次。原始数据、归一化统计与 checkpoint 的版本一致且可追溯。

**未通过**：按接口、环境、配置或模型定位复现问题；交付当前可复现版本和明确限制，不能把只在开发者交互会话中运行的演示称为完成交接。

**第二周缓冲**：优先用于修复、独立重新验收或补齐交付资料。若没有足够真机时间重新做完整验收，按未达成原验收目标报告。

## 4. 人类视频与现有重建怎样形成研究结果

本期可验收的研究结果是：把人类视频中的操作阶段和可见接触区域，转成机器人示教、状态转换判据和错误分析的依据；用实际托盘尺寸把参考资产落到物理尺度。

你给出的论文是综述，梳理任务、观测与动作等迁移接口，不是一份可以直接执行的 R1 Pro 控制配方。本文优先取“任务阶段/接触信息”，因为裸手视频不直接提供机器人控制动作，视频也不能可靠给出夹持力。参考 [Robot Learning from Human Videos: A Survey](https://arxiv.org/html/2604.27621v1)。

明确交付：5 段托盘人类视频、六阶段标注、已知/未知接触区域、阶段到机器人动作的映射表、人工核验记录。保留当前 TRELLIS.2 GLB 作为参考；遮挡补全和纹理效果不决定控制几何。规则托盘优先使用量尺建立的简单几何，碰撞/接触面应保留开口和边缘的实际形状。

**本期不能声称已经证明“人类视频减少了多少机器人数据”。** 若主线提前通过 G8，可设计下一阶段的受控实验：固定机器人数据量、训练设置和测试初态，对比 ACT 基线与明确增加了阶段/接触条件的变体，再做多次训练与独立真机评估。仅看过视频或生成了 GLB 不构成这个对照；π 系列的对比也须使用同样清楚的数据和试验预算。

## 5. 结果分级与交付目录

| 最终状态 | 可以报告的结论 | 不可以混淆的内容 |
|---|---|---|
| G1–G10 全部通过 | 学习策略已在规定空托盘工位完成真机部署和项目验收 | 不代表开放机场场景或任意托盘泛化 |
| ACT 已上机但未通过 G9/G10 | 已完成真机闭环，距离验收还差哪些指标 | 不把少数成功录像当成达标 |
| 仅 G2 固定工况基线可靠 | 固定工况控制基线已部署；学习路线尚未达到目标 | 不把示教航点执行说成模型学会搬运 |
| 控制/机械条件仍未通过 | 已定位接口或机械阻碍，交付证据和后续条件 | 不承诺模型训练能绕过这些阻碍 |

计划交付目录如下，除本计划文件外，内容均待执行阶段产生：

```text
r1pro_tray/
  config/       # 任务规格、接口、相机、动作与执行限制
  data/         # 原始记录索引、转换脚本、数据质量、划分清单
  policies/     # ACT 权重、归一化统计、训练配置与代码版本
  deploy/       # 机器人适配器、执行器、推理服务、启动/停止流程
  eval/         # 开发与正式测试分别存放；逐次结果、视频和故障日志
  research/     # 人类视频标注、资产实测、路线选择与后续对照设计
  README.md     # 安装、采集、训练、部署、重启、停止、恢复和复现步骤
```

**每天收工必须回答四件事**：今天门槛通过没有；证据在哪里；失败属于机械/控制/数据/模型哪一层；明天先解决哪一个阻碍。目标日期不改变验收含义。
