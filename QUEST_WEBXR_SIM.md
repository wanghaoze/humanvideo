# Quest 3 实时仿真画面与遥操

2026-09-15：服务器已部署小臂水平、夹爪张开的初始姿态。静置验证两侧小臂倾角小于 1 度，夹爪总开度约 0.10 米。服务器画面为 640 × 480。

## 启动

Quest 3 保持 USB 连接电脑，并允许 USB 调试。在项目根目录 PowerShell 运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/start_quest_webxr.ps1
```

戴上头显唤醒，在 Quest Browser 点击“进入 VR”并允许 VR 权限。前方屏幕显示服务器单目仿真画面。按 Y 或 ENABLE 启用控制；按住中指侧握键小幅移动对应机械臂，食指扳机控制闭合。松开侧握键保持目标，B 或 PAUSE 暂停，EXIT VR 返回浏览器。

画面超过 1.5 秒未更新时停止发送运动输入并请求暂停；服务器输入超过 0.25 秒未更新时保持目标。USB 和本机 relay 必须持续连接。此页面只控制仿真，不启动示教录制。

## 当前验收状态

- 已验证：服务器新姿态、稳定性、无新增机器人碰撞；相关 9 项测试通过。
- 已验证：本机 relay 可读取服务器状态和 JPEG 画面，JavaScript 语法检查通过。
- 待用户戴上头显验证：Quest Browser 页面加载、进入 WebXR、头显内画面、双手柄实时输入。
- 原生 APK 的双手柄遥操在前一轮已验证，但不能替代本次 WebXR 链路验收。

当前服务器保持暂停。临时 SSH 授权及本机临时密钥已在部署后删除。此初始姿态调整不等于完成托盘抓取；现有场景的抓取可达性还需要另行调整和验证。

## 后上方视角与场景重置（已部署）

监视相机位于世界坐标 (0.1, 0, 2.8) 米，从机器人后上方看向桌面 (1.0, 0, 0.8)。机器人底座保持 (0.5, 0, 0)。

Quest 点击 RESET，或右手柄长按 A 一秒，发送 reset_paused；电脑网页按 R 同样重置。重置恢复机器人与托盘初始状态并暂停，按 Y/ENABLE 重新启用。页面不会在正在录制时执行此重置。

已验证服务器及 USB relay 的 reset_paused 接口、初始关节目标与张开夹爪、暂停状态以及服务器新视角截图。8 项现有协议测试通过；Quest 实际手柄 A 键和 VR 点击仍需佩戴测试。临时 SSH 授权已删除。

## 多视角与控制调整（本地完成，待服务器部署）

默认四宫格：后上方、前侧方、左腕、右腕。Quest 按 X 或 VIEW 在四宫格和各单画面间循环。腕部监视相机位于夹爪上方，面向夹指前方接触区；这是仿真监视安装参数，不代表真机相机标定。

位置倍率从 0.7 改为 1.5，旋转倍率为 1.3。侧握键超过 0.6 开始、低于 0.35 松开，减少临界值反复跳动。加入 1.5 毫米位置、0.5 度旋转噪声保持区及随运动幅度变化的低通滤波。机械臂关节速度上限 1.5 rad/s，腕部三个关节 2.2 rad/s，加速度限制 8 rad/s²。限速仍然适用，不保证任意动作都严格按倍率到达。工作范围随底座前移修正；保持松握、暂停和输入过期时的目标保持。

监视渲染使用独立 MuJoCo 数据和渲染线程，所有四路画面来自同一物理状态快照；慢视频不阻塞主控制循环。多视角视频最多 10 FPS，实际帧率取决于渲染和网络。录制路径保留同步采集。HTTP 帧携带状态快照年龄，网页拒绝过旧图像。

本地验证：11 项测试通过，包含噪声保持、1.5 倍平移、1.3 倍旋转、侧握滞回、真实 IK 位姿跟随和超时保持；五种视图接口以及 reset_paused 验证通过。尚需服务器性能测量和用户戴头显试操评价手感。

### 服务器部署验收

多视角及控制优化已部署。五种视图均返回独立画面，Quest USB relay 四宫格和画面年龄头验证通过。渐进输入测试先保持手柄建立控制起点，再平移 4 厘米、旋转 0.2 rad；测得末端平移 5.825 厘米、旋转 0.249 rad。空闲和测试期间控制循环约 1–2 毫秒，四路渲染约 15 毫秒。首轮一步突变探针未达到期望，已另存诊断；采用保持起点和渐进输入的重测通过，见 runs/multiview_control/server_motion_validation.json。服务已重置并暂停，等待头显手感测试。
